"use client";
import {useRef,useState} from "react";
import type {FFmpeg as FFmpegType} from "@ffmpeg/ffmpeg";

type Preview={title:string;author:string;thumbnail:string;url:string};
const MAX_FILE_SIZE=200*1024*1024;

export default function Converter({format="mp3"}:{format?:"mp3"|"wav"}){
 const[quality,setQuality]=useState("192");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);const[file,setFile]=useState<File|null>(null);const[url,setUrl]=useState("");const[preview,setPreview]=useState<Preview|null>(null);const[progress,setProgress]=useState(0);const[downloadUrl,setDownloadUrl]=useState("");const[downloadName,setDownloadName]=useState("audiosnap-audio");
 const fileRef=useRef<HTMLInputElement>(null);const ffmpegRef=useRef<FFmpegType|null>(null);
 function clearDownload(){if(downloadUrl)URL.revokeObjectURL(downloadUrl);setDownloadUrl("")}
 async function getFFmpeg(){const[{FFmpeg},{toBlobURL}]=await Promise.all([import("@ffmpeg/ffmpeg"),import("@ffmpeg/util")]);let ffmpeg=ffmpegRef.current;if(!ffmpeg){ffmpeg=new FFmpeg();ffmpeg.on("progress",({progress})=>setProgress(Math.max(0,Math.min(100,Math.round(progress*100)))));const base="https://cdn.jsdelivr.net/npm/@ffmpeg/core@0.12.10/dist/umd";await ffmpeg.load({coreURL:await toBlobURL(`${base}/ffmpeg-core.js`,"text/javascript"),wasmURL:await toBlobURL(`${base}/ffmpeg-core.wasm`,"application/wasm")});ffmpegRef.current=ffmpeg}return ffmpeg}
 async function convertSource(source:File,name:string){setLoading(true);setProgress(0);clearDownload();try{if(source.size>MAX_FILE_SIZE)throw new Error("File is too large. Please use a file under 200 MB.");setMessage("Loading secure browser converter…");const[{fetchFile},ffmpeg]=await Promise.all([import("@ffmpeg/util"),getFFmpeg()]);setMessage("Converting on your device…");const ext=source.name.split(".").pop()?.replace(/[^a-zA-Z0-9]/g,"")||"media";const input=`input.${ext}`;const output=format==="wav"?"audiosnap-output.wav":"audiosnap-output.mp3";await ffmpeg.writeFile(input,await fetchFile(source));const args=format==="wav"?["-i",input,"-vn","-c:a","pcm_s16le",output]:["-i",input,"-vn","-c:a","libmp3lame","-b:a",`${quality}k`,output];await ffmpeg.exec(args);const data=await ffmpeg.readFile(output);const bytes=data instanceof Uint8Array?data:new TextEncoder().encode(data);const blob=new Blob([bytes as BlobPart],{type:format==="wav"?"audio/wav":"audio/mpeg"});setDownloadUrl(URL.createObjectURL(blob));setDownloadName(name.replace(/\.[^.]+$/,"").replace(/[^a-zA-Z0-9-_ ]/g,"").trim()||"audiosnap-audio");setProgress(100);setMessage("Conversion complete. Your download is ready.");await ffmpeg.deleteFile(input);await ffmpeg.deleteFile(output)}catch(e:any){console.error(e);setMessage(e?.message||"Conversion could not be completed. Try another supported media file.")}finally{setLoading(false)}}
 async function checkYouTube(e:React.FormEvent){e.preventDefault();setLoading(true);setPreview(null);setMessage("Checking YouTube video…");clearDownload();try{const r=await fetch("/api/youtube-preview",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url})});const data=await r.json();if(!r.ok)throw new Error(data.error||"Could not load video.");setPreview(data);setMessage("Video found. If you own or are authorized to use it, choose your local copy below to convert it.")}catch(e:any){setMessage(e?.message||"Could not load the YouTube video.")}finally{setLoading(false)}}
 async function convertLocal(){if(!file){fileRef.current?.click();return}await convertSource(file,preview?.title||file.name)}
 return <div id="converter" className="converter-card">
  <form onSubmit={checkYouTube} className="url-converter"><div className="status-row"><strong>YouTube video</strong><span className="live-badge">URL Check</span></div><p>Paste a YouTube video URL to load its title and thumbnail, then select your own or authorized copy for audio conversion.</p><label htmlFor="youtube-url">YouTube URL</label><div className="input-row"><input id="youtube-url" value={url} onChange={e=>{setUrl(e.target.value);setPreview(null)}} placeholder="https://www.youtube.com/watch?v=..." type="url" required disabled={loading}/></div><button type="submit" className="button wide" disabled={loading||!url.trim()}>{loading?"Checking…":"Check YouTube Video"}</button></form>
  {preview&&<div className="video-preview"><img src={preview.thumbnail} alt="YouTube video thumbnail"/><div><span className="preview-label">VIDEO FOUND</span><h3>{preview.title}</h3><p>{preview.author}</p><p className="preview-note">To convert this video, select a copy you own or are authorized to process.</p></div></div>}
  <div className="divider"><span>{preview?"SELECT YOUR VIDEO COPY":"OR CONVERT A FILE"}</span></div>
  <div className="local-converter"><h3>{preview?`Convert “${preview.title}”`:"Convert your own file"}</h3><p>Choose an audio or video file from your device. The file is processed locally in your browser.</p>{format==="mp3"&&<div className="local-quality"><label>MP3 quality</label><select value={quality} onChange={e=>setQuality(e.target.value)} disabled={loading}><option value="128">128 kbps</option><option value="192">192 kbps</option><option value="256">256 kbps</option><option value="320">320 kbps</option></select></div>}<input ref={fileRef} className="file-input" type="file" accept="audio/*,video/*" onChange={e=>{const f=e.target.files?.[0]||null;setFile(f);setProgress(0);setMessage(f?`Selected: ${f.name}`:"");clearDownload()}}/><button type="button" className="ghost wide-ghost" disabled={loading} onClick={()=>fileRef.current?.click()}>{file?`Selected: ${file.name}`:"Choose authorized video/audio file"}</button>{file&&<button type="button" className="button wide" disabled={loading} onClick={convertLocal}>{loading?`Converting… ${progress}%`:`Convert to ${format.toUpperCase()}`}</button>}</div>
  {loading&&progress>0&&<div className="progress-track"><div className="progress-fill" style={{width:`${progress}%`}}/></div>}{downloadUrl&&<a className="download-button" href={downloadUrl} download={`${downloadName}.${format}`}>Download {format.toUpperCase()}</a>}{message&&<div className="alert">{message}</div>}<p className="notice">YouTube URL checking loads public metadata only. Audio conversion requires a media file you own or are authorized to process.</p>
 </div>
}
