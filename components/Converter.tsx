"use client";import {useState} from "react";export default function Converter({format="mp3"}:{format?:"mp3"|"wav"}){const[url,setUrl]=useState("");const[quality,setQuality]=useState("192");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage("");try{const r=await fetch("/api/convert",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({url,format,quality})});const d=await r.json();if(!r.ok)throw new Error(d.message||"Conversion failed.");setMessage(d.downloadUrl?`Ready: ${d.downloadUrl}`:"Conversion completed.")}catch(e:any){setMessage(e.message)}finally{setLoading(false)}}return <div id="converter" className="converter-card"><form onSubmit={submit}><label>YouTube URL</label><div className="input-row"><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://www.youtube.com/watch?v=..." required/></div><div className="options"><div><label>Audio quality</label><select value={quality} onChange={e=>setQuality(e.target.value)}><option value="128">128 kbps</option><option value="192">192 kbps</option><option value="256">256 kbps</option><option value="320">320 kbps</option></select></div><div className="format-pill">{format.toUpperCase()}</div></div><button className="button wide" disabled={loading}>{loading?"Processing…":`Convert to ${format.toUpperCase()}`}</button></form>{message&&<div className="alert">{message}</div>}<p className="notice">Use this tool only with content you are authorized to download or convert.</p></div>}
